# Abbreviations

abbr -ag dc "docker compose"
abbr -ag g "git"
abbr -ag gc "git commit"
abbr -ag p "pnpm"
abbr -ag n "npm"
abbr -ag tf "terraform"
abbr -ag avx "aws-vault exec"
abbr -ag avl "aws-vault login"
abbr -a drun "docker run --rm -it"
abbr -a yt "yt-dlp"
